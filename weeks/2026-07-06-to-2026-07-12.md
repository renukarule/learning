# Week of July 6 – July 12, 2026

## Tuesday, July 7

- **Claude model variants** — Compared Opus, Sonnet, Haiku, and Fable on capability vs. speed vs. cost; settled on Sonnet for day-to-day coding work.
- **LLM overview doc** — Wrote a reference document covering tokenization, transformer architecture, training stages, and inference settings — first as a styled page, then as plain Markdown.
- **Custom Claude Code skill** — Built a project skill (`daily-history-log`) that appends dated session summaries to a log file.
- **Scheduling** — Learned cloud `/schedule` routines can't reach local files; chose Windows Task Scheduler for a local daily trigger instead.
- **GitHub setup** — Connected the GitHub connector on claude.ai, then installed and authenticated the GitHub CLI (`gh`) locally.
- **First push** — Initialized a local git repo and pushed it as a new private GitHub repository.
- **Connector debugging** — Diagnosed a `403 push denied` error and learned claude.ai's GitHub connector is read-only by design; writes need Claude Code or a GitHub MCP server instead.

## Wednesday, July 8

- **Git setup** — Installed Git for Windows via winget and set global `user.name` / `user.email` for commits.
- **Practice trading-app UI** — Built a Robinhood-inspired static landing page for UI practice, then extended it into a working no-login paper-trading demo with buy/sell, live price ticks, and localStorage persistence.

## Thursday, July 9

- **Portfolio site** — Designed and built a personal introduction site from scratch, with a palette and typography chosen for an ML/NLP profile.
- **Deployment** — Pushed the site to a new GitHub repo, installed the Netlify CLI, and deployed it live.
- **Continuous deployment** — Linked the GitHub repo to Netlify so every push to `master` auto-deploys the site; verified the link via the Netlify API.
